<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- La aplicacion no sirve de nada sin la API. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- configChanges uiMode|locale en cada pantalla: el tema y el idioma no
         las recrean (eso parpadea); BaseActivity las reinicia con fundido. -->
    <application
        android:name=".FinanceTrackerApp"
        android:localeConfig="@xml/locales_config"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinanceTracker">
        <activity
            android:name=".LoginActivity"
            android:configChanges="uiMode|locale|layoutDirection"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RegisterActivity"
            android:configChanges="uiMode|locale|layoutDirection"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".BudgetActivity"
            android:configChanges="uiMode|locale|layoutDirection"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".MainActivity"
            android:configChanges="uiMode|locale|layoutDirection"
            android:exported="false" />

        <activity
            android:name=".NewTransactionActivity"
            android:configChanges="uiMode|locale|layoutDirection"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Guarda el idioma elegido en la app en Android 12 y anteriores.
             En 13+ lo guarda el sistema. -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>
    </application>

</manifest>